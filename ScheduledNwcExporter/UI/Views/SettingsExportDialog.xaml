<Window x:Class="ScheduledNwcExporter.UI.Views.SettingsExportDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Export Settings" Height="190" Width="400"
        WindowStartupLocation="CenterOwner" Background="#FFF5F5F5" ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Export Hatco NWC Exporter Configuration" FontSize="14" FontWeight="Bold" Margin="0,0,0,10"/>
        
        <CheckBox Grid.Row="1" x:Name="IncludeJobsCheckBox" Content="Include Model List (Queue) in the export file" IsChecked="True" Margin="0,0,0,10"/>
        <TextBlock Grid.Row="2" Text="If checked, team members importing this file will also receive your configured model export queue." FontSize="11" Foreground="Gray" TextWrapping="Wrap" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content=" OK " Click="OkButton_Click" Width="80" Height="28" Margin="0,0,8,0" FontWeight="Bold"/>
            <Button Content=" Cancel " Click="CancelButton_Click" Width="80" Height="28"/>
        </StackPanel>
    </Grid>
</Window>
